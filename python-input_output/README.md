# python-input_output

This project covers file input/output in Python: reading, writing, and
appending to text files with the `with` statement, JSON serialization
and deserialization (strings and files), building a class-to-JSON
serialization/deserialization mechanism with the Student class, and a
Pascal's triangle interview-prep exercise.
