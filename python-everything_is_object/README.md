# python-everything_is_object

This project explores how Python treats everything as an object: identity
(`id`), type (`type`), references, aliasing, and the difference between
mutable objects (list, dict, set, bytearray) and immutable objects
(int, float, complex, str, tuple, frozenset, bytes) — including how each
behaves when passed to functions.
