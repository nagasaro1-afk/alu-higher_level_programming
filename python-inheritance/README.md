# python-inheritance

This project covers Python inheritance concepts: introspection with
dir(), comparing types with type() and isinstance(), building a class
hierarchy (BaseGeometry -> Rectangle -> Square), and validating
instance attributes across parent and child classes.
